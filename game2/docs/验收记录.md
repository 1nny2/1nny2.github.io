# 验收记录 · 2026-09-10

## v2.0 基线自动测试

`node --test tests/*.test.js`：23 项通过，0 项失败。

覆盖：30/60/144 帧一致的运动结果；双方子弹归属和命中；出生保护；正面护盾与背后受击；冲刺碰撞；临时掩体；砖/钢/水；基地友伤保护；地图对称及可达；三次击毁胜利；超时结算；五波守卫；非法输入与类型；静态资源白名单；成员权限；反复短按/序号幂等；输入过期自动停止；掉线和退出；双端结算；双人同意再战；排位分与好友房隔离；游客档案持久化。

`node --check client.js` 与 Python 启动器 AST 解析通过；`python tank_server.py` 已实际启动服务。

## 浏览器流程

使用内置浏览器，`localhost` 与 `127.0.0.1` 两个独立站点存储身份连接同一个服务器。

- 首页加载与战车、地图选择界面可见；品牌文字保留。
- 单人守卫运行、基地失败结算、再次出击、P 暂停通过。
- 好友房创建 → 第二身份加入 → 双方准备 → 同步开局通过。
- 触屏按钮的轻点技能显示真实冷却，短按事件不会被两次轮询间隙吞掉。
- 对局中刷新可恢复同房及时间、技能冷却状态。
- 完整运行一局三分钟，两个浏览器都显示相同平局结果。
- 一方请求再战后按钮保持禁用等待；双方同意后自动从交错前线换至钢铁回廊。
- 一方退出对局，另一方显示胜利及“对手离开了对局”。
- 最终版本战斗音效按钮及暂停按钮正常，浏览器错误日志为空。

三分钟实测曾显示 `181s`，原因是浮点时间取整；已将竞技逻辑时间限制为 180 秒，测试补充精确时间断言。

## 响应式

- 桌面默认约 1280×720。
- 390×844 竖屏：页面无横向溢出；战场与下方触控区分开，无覆盖。
- 667×375 横屏：页面宽高均未超出视口；战场位于中间，触控区分布两侧。
- 补充手机战斗内音效开关和横屏安全区内边距。

这是浏览器视口验收，没有冒称已测试实体 iPhone/Android、多点触控硬件或公网弱网。

## 部署状态与边界

- 本地和局域网服务可运行，未进行公网部署或并发容量承诺。
- 当前匹配为先到先匹配；需要两名真实玩家，无虚构在线人数或机器人排位填充。
- 游客身份、单进程内存房间、JSON 档案存储适合首版验证；账号找回、防刷分、分段匹配、弱网/压测仍是后续工作。
- 玩家长期成长目前体现为熟练度与战车收藏；所有战车可试玩，排位全部开放且没有永久数值加成。


## v2.1 本轮验收

`node --test tests/*.test.js`：35 项通过，0 项失败。`node --check client.js` 通过。

新增覆盖：3 秒冻结倒计时；不同生命上限的百分比判定；30/60/144 FPS 路口转向不穿墙；工程放置预览与执行一致；格挡/命中/敌方掩体拦截分别统计；练习不生成敌人；专精一次性奖励、重复报告去重与最近 8 场上限；双方载入确认、旧 matchId 拒绝、倒计时输入忽略、开局前退出不扣分；持续轮询但未载入的客户端也在 20 秒后取消。

浏览器实际流程（localhost 与 127.0.0.1 两个站点身份）：

- 首次教学提示、开始训练、方向移动一格、射击、施放技能、完成教学、返回大厅。档案未增加教学专精或战绩。
- 工程车参与快速排位，选择边练边等，等待状态持续显示；另一身份加入，练习自动切为对局、显示倒计时。
- 双方坦克与 HUD 正确区分「你」，工程车显示 3/3 · 100%，防御车显示 4/4 · 100%。
- 工程掩体施放显示 8 秒冷却；刷新恢复相同房间、作战时间与冷却。
- 完整 180 秒对局结束，两端均为平局，显示血量百分比结算与 180s。
- 刷新已结束房间后返回档案，只保留一条本局战绩；熟练度未重复增加。
- 390×844：页面尺寸 390×844，战场 366×366，触控区在战场下方；手机技能按钮轻点进入真实冷却。
- 667×375：页面尺寸 667×375，战场约 248×248，触控按钮分布两侧，无横向溢出。
- 浏览器错误日志为空。视口测试不代表实体手机、硬件多点触控或弱网测试。

本轮新增训练/成长主要是本地体验，排位判定仍来自服务器。成长状态及本地熟练度不是防篡改数据，不用于增加排位战斗属性。

补充回归：取消匹配后保留自由练习；正式开局后主动退出，退出方档案出现失利战绩并刷新积分，对方得到胜利结算；工程坦克朝钢墙施放技能显示「地形阻挡，换个位置 · 未消耗冷却」，技能仍为就绪。最后恢复作战大厅并保持服务运行。
